Document Object Model: 
Programming interface for HTML and XML documents, represents page so programs can change document structure, style, content
Browser's live, structured representation of web page, tree of elements (tags, text, attributes), JS can read and change