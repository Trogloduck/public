**Up/Down**: order in which installed extensions are invoked when processing traffic (top to bottom)

**Jython:** extension to run Python based extensions

https://www.jython.org/download
Extensions > Extensions settings > Python environment > Set Jython JAR Location

Burp supports Java (native), Python (via Jython), Ruby (via JRuby)