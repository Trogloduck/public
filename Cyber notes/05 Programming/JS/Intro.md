[[JavaScript Essentials]]

1° Open Inspector in Web Browser > Open Console
*Good option to test code snippets, but not to write longer code*

`"Hello World"`: prints "Hello World" in the console

`alert("Hello World"!);`: creates pop up with "Hello World!" in it

`console.log();` displays output of code

Ex:
```JS
for (var i = 0; i < 10; i++) {
	console.log(i);
}
```
*prints numbers 1 to 10*